[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    coverage
    -r requirements.debug.txt
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = firstvoices/
branch = True
